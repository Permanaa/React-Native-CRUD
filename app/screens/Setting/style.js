import {StyleSheet} from 'react-native'
import {
    COLOR_TEXT
} from '../../styles'

const styles = StyleSheet.create({
    container: {
        margin: 30
    },
    text_color2: {
        color: COLOR_TEXT
    },
    title: {
        fontSize:30,
        fontWeight: 'bold',
        marginTop: 20
    }
})

export default styles